# Secretos y config local — nunca subir
backend/.env
backend/*.env
backend/firebase-service-account.json
*.pem
*.cer
*.key

# Backend (Python)
backend/venv/
backend/__pycache__/
**/__pycache__/
*.pyc

# Archivos temporales / descargas grandes que no son parte del proyecto
*.zip
jdk-*/

# Mobile (Flutter)
mobile/build/
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/android/local.properties
mobile/android/.gradle/
mobile/android/app/build/
mobile/ios/Pods/
mobile/ios/.symlinks/

# Herramientas / IDE
.codegraph/
.cursor/
.claude/

# Repo aparte (Azure DevOps de la empresa), clonado dentro por error/comparacion
app-control-data/
